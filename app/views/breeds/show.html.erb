<h1><%= @breed.name %></h1>
<h2>Images</h2>
<ul>
  <% @breed.images.each do |image| %>
    <li><%= link_to image_tag(image.url, alt: @breed.name), breed_image_path(@breed, image) %></li>
  <% end %>
</ul>
