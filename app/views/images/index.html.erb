<!DOCTYPE html>
<html>
<head>
  <title>Dog Images</title>
  <style>
    .image-container {
      display: flex;
      flex-wrap: wrap;
    }
    .image-item {
      margin: 10px;
      text-align: center;
    }
    .image-item img {
      max-width: 200px;
      height: auto;
    }
  </style>
</head>
<body>
  <h1>All Dog Images</h1>

  <div class="image-container">
    <% @images.each do |image| %>
      <div class="image-item">
        <h3><%= image.breed.name %></h3>
        <img src="<%= image.url %>" alt="<%= image.breed.name %>">
      </div>
    <% end %>
  </div>
</body>
</html>